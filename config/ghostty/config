# Ghostty keybindings: Windows-style copy/paste
# Copy only consumes Ctrl+C when a selection exists
keybind = performable:ctrl+c=copy_to_clipboard
# Paste from system clipboard
keybind = ctrl+v=paste_from_clipboard
